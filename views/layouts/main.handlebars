<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
  <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.7.1/css/all.css" integrity="sha384-fnmOCqbTlWIlj8LyTjo7mOUStjsKC4pOpQbqyi7RrhN7udi9RwhKkMHpvLbHG9Sr"
    crossorigin="anonymous">
  <link rel="stylesheet" href="/styles/styles.css" media="screen" title="no title" charset="utf-8">
  <title>Gimme Books</title>
</head>

<body>
  <header>
    <nav class="nav-extended blue grey darken-1">

      <div class="nav-wrapper container">
        <a href="/" class="brand-logo">Gimme◬Books</a>

        {{#if loggedIn}}
        <ul class="right hide-on-med-and-down">
          <li>
            <a href="/logout" class="btn btn-large waves-effect waves-red blue-grey darken-5"><i class="material-icons">exit_to_app</i></a>
          </li>
        </ul>
        
        <span class="right hide-on-med-and-down">Welcome, <a href="/user/{{id}}" data-user="{{id}}">{{user}}</a>!</span>

        {{else}}
        <ul id="login" class="right hide-on-med-and-down">
          <li>
            <a href="#modal" class="btn waves-effect waves-light modal-trigger">Login</a>
            <div id="modal" class="modal">
              <div class="modal-content">
                <input type="email" id="loginEmail" placeholder="Enter email" name="email">
                <input type="password" id="loginPassword" placeholder="Enter password" name="password">
                <label>
                  <input type="checkbox">
                  <span>Remember Me</span>
                </label>
                <button class="btn right" type="submit" id="loginButton">Submit</button>
              </div>
            </div>
          </li>
        </ul>
        {{/if}}
        <a href="#" data-target="mobile-menu" class="sidenav-trigger right"><i class="material-icons">menu</i></a>
      </div>
    </nav>

    <ul class="sidenav" id="mobile-menu">
      {{#if loggedIn}}
      <li><a href="/user/{{id}}" data-user="{{id}}"><i class="material-icons">face</i>{{user}}</a></li>
      <li>
        <a href="/logout" class="btn btn-large waves-effect waves-red blue-grey darken-5"><i class="material-icons">exit_to_app</i></a>
      </li>
      {{else}}
      <li id="mobileLogin">
        {{!-- <a href="#modal" class="btn waves-effect waves-light modal-trigger">Login</a> --}}
        <input type="email" id="loginEmailM" placeholder="Enter email" name="email">
        <input type="password" id="loginPasswordM" placeholder="Enter password" name="password">
        <label>
          <input type="checkbox">
          <span>Remember Me</span>
        </label>
        <button class="btn right" type="submit" id="loginButtonM">Submit</button>
      </li>
      {{/if}}
    </ul>
  </header>

  {{{body}}}
  <script src="https://code.jquery.com/jquery-3.3.1.min.js" integrity="sha256-FgpCb/KJQlLNfOu91ta32o/NMZxltwRo8QtmkMRdAu8="
    crossorigin="anonymous"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
  <script src="/js/Favorite.js"></script>
  <script src="/js/index.js"></script>
  <script src="/js/books.js"></script>
</body>

</html>